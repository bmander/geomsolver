[workspace]
resolver = "2"
members = ["gcs-core", "gcs-ffi", "gcs-cli"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
# not "abort": the FFI's `guard` turns a core panic into an error return.
panic = "unwind"
